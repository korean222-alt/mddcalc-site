# mddcalc.com 작업 규칙

이 저장소를 고칠 때 매번 지켜야 하는 것들입니다.
자세한 인수인계는 `docs/progress.md` 를 보세요.

---

## 1. 고쳤으면 배포까지가 한 세트다

**코드를 고치고 멈추지 않는다. 배포가 끝나고 실제로 뜨는 것까지 확인한 다음에 보고한다.**

커밋만 하고 "완료했습니다" 라고 하면 안 됩니다. 사용자가 보는 것은 저장소가 아니라
mddcalc.com 입니다. 커밋은 됐는데 배포가 깨져 있으면 사용자 입장에서는 아무것도 안 고쳐진
겁니다. 실제로 `vercel.json` 에 JSON 주석(`"//"`)을 넣었다가 스키마 검증에서 막혀
배포가 통째로 실패한 적이 있습니다. 커밋은 멀쩡했고, 사이트는 그대로였습니다.

순서:

1. 고친다
2. **검증한다** — 아래 3항의 테스트를 돌리고, 화면이 바뀌는 변경이면 브라우저로 직접 본다
3. 커밋 → 지정된 브랜치로 푸시
4. **배포 결과를 확인한다** — 빌드가 성공했는지, 바뀐 페이지가 실제로 뜨는지
5. 실패했으면 고쳐서 다시 4번으로. 성공한 것까지 보고 나서 보고한다

배포가 실패했는데 원인을 못 잡겠으면, 그 사실과 오류 메시지를 그대로 말합니다.
"푸시했습니다" 로 끝내지 않습니다.

### 배포 확인이 막힐 때

샌드박스에서는 확인 자체가 안 될 수 있습니다. 실제로 겪은 두 가지:

- **mddcalc.com 이 egress 프록시에 막힘** — `curl` 도 `WebFetch` 도 403.
- **Vercel MCP 에 이 프로젝트가 안 보임** — 연결된 계정에는 다른 프로젝트만 있었습니다.
  (`list_projects` 로 확인 가능)

이때는 **확인했다고 말하지 마세요.** 대신:

1. 확인할 수 있는 것까지 확인한다 — JSON 유효성, 스키마가 허용하는 키만 썼는지,
   로컬 브라우저에서 화면이 제대로 나오는지
2. **확인하지 못한 것을 그대로 말한다** — 무엇이 막혀서 무엇을 못 봤는지
3. 사용자에게 어디를 봐 달라고 구체적으로 짚어 준다
   (Vercel 대시보드의 해당 배포, 또는 실제 페이지 주소)

또 하나: **mddcalc.com 은 `main` 을 서빙합니다.** 작업 브랜치에 푸시하면 프리뷰 배포만
돕니다. 실제 사이트에 반영되려면 `main` 에 병합돼야 합니다 — 브랜치 푸시를 두고
"사이트에 반영됐습니다" 라고 하면 안 됩니다.

### vercel.json 주의

`vercel.json` 은 스키마 검증을 통과해야 배포가 됩니다.

- **JSON 에는 주석이 없습니다.** `"//"` 같은 키를 넣으면
  `should NOT have additional property //` 로 배포 전체가 실패합니다.
- 설명을 남기고 싶으면 이 파일이나 `docs/progress.md` 에 씁니다.
- 최상위에 쓸 수 있는 키는 `$schema`, `headers`, `redirects`, `rewrites`, `routes` 등
  Vercel 이 정한 것뿐입니다. 임의의 키를 추가할 수 없습니다.

---

## 2. 데이터가 언제 것인지, 언제 갱신되는지

이 사이트의 시세는 **실시간이 아닙니다.** 조회할 때마다 새로 계산하지 않습니다.

### 섹터 RS · 히트맵 (`data/sectors.json`)

| | |
|---|---|
| 무엇을 보여주나 | **마지막으로 마감된 거래일의 종가** |
| 언제 갱신되나 | 평일 **한국시간 18:30** 과 **다음날 07:30** (하루 두 번) |
| 무엇이 하나 | `.github/workflows/refresh-kr-data.yml` (GitHub Actions) |
| 어떻게 | 네이버 금융(한국) · Twelve Data(미국) 일봉을 받아 미리 계산 → `data/sectors.json` 커밋 |
| 브라우저는 | 그 파일 하나(약 130KB)를 읽을 뿐, 계산하지 않음 |

- **18:30 실행**에서 한국 데이터가 그날 종가로 갱신됩니다.
- **07:30 실행**은 미국장 마감 뒤라 미국 데이터가 갱신됩니다.
  그래서 미국 데이터는 한국 시각 기준으로 **한 거래일 늦게** 반영됩니다.
- 주말·공휴일에는 돌지 않습니다. 월요일 아침에 보는 것은 금요일 종가입니다.
- 수집이 실패한 종목은 지난 데이터를 유지하고 나머지만 갱신됩니다.
  한쪽 시장이 통째로 실패해도 다른 시장은 살아 있습니다.

### 종목 리포트 (`stock/*.html`)

주 1회, 월요일 03:00 UTC (`refresh-stock-pages.yml`). Twelve Data API 키가 필요합니다.

### 장중 실시간은 지원하지 않는다

무료 소스로는 불가능합니다. 실시간을 하려면 유료 시세 API 와 서버가 필요하고,
지금 구조(정적 파일 + Vercel 정적 호스팅)를 통째로 바꿔야 합니다.
**"실시간처럼 보이게" 만들지 마세요.** 화면에는 항상 기준일이 적혀 있어야 하고,
기준일이 오래되면 그 사실이 눈에 띄어야 합니다
(`assets/site.js` 의 `rsStaleNote()` — 4일 넘게 밀리면 경고).

### 캐시

`data/*` 는 `vercel.json` 에서 `max-age=0, must-revalidate` 로 내려갑니다.
브라우저도 `fetch(..., { cache: 'no-cache' })` 로 매번 서버에 확인합니다.
안 바뀌었으면 304 라 트래픽은 늘지 않습니다.
**갱신됐는데 어제 숫자가 보이는 일**을 막기 위한 것이니 지우지 마세요.

---

## 3. 고친 뒤 돌릴 것

```bash
node scripts/test-sector-rs.js    # 섹터 RS 산식 (눈으로는 틀린 걸 알 수 없는 숫자들)
node scripts/test-kr-parse.js     # 한국 시세 파서
node scripts/test-us-parse.js     # 미국 시세 파서
node scripts/test-nav-links.js    # 네비게이션 링크 (표가 두 곳에 복제돼 있음)
```

`scripts/sectors.js`(섹터·그룹 정의)나 `scripts/generate-sector-rs.js`(산식)를 고쳤으면
데이터도 다시 만들어 함께 커밋합니다. 네트워크를 쓰지 않습니다.

```bash
node scripts/generate-sector-rs.js
```

화면이 바뀌는 변경이면 로컬에서 직접 봅니다. 외부 CDN(chart.js, adsense)은 샌드박스에서
막히므로 Chart 는 스텁을 주입해 확인합니다.

```bash
python3 -m http.server 8899    # 저장소 루트에서
```

---

## 4. 광고

**쿠팡 파트너스는 2026-08-23 에 전부 제거했습니다** (애드센스 심사). 다시 넣지 마세요.
저장소에 쿠팡 코드는 한 줄도 없어야 합니다.

```bash
grep -rniE "쿠팡|coupang|ads-partners" --include="*.html" --include="*.js" . | grep -v node_modules
```

`scripts/generate-blog-pages.js` 와 `scripts/generate-stock-pages.js` 는 HTML 을
생성합니다. 배너를 지울 때 여기를 빠뜨리면 생성기를 돌리는 순간 되살아납니다.

---

## 5. 잘 틀리는 자리

- **`assets/site.js` 와 `index.html` 에 PAGE_URLS 표가 복제돼 있습니다.**
  한쪽만 고치면 링크가 죽는 대신 홈으로 되돌아와서 "눌러도 아무 일이 없는" 것처럼 보입니다.
  `test-nav-links.js` 가 잡아 줍니다.
- **`data/sectors.json` 은 생성물입니다.** 손으로 고치지 말고 생성기를 돌리세요.
- **없는 값을 0 으로 채우지 마세요.** 화면에 나오면 누군가는 그걸 읽습니다.
  외국인 소진율은 결제일(T+2) 뒤 확정이라 마지막 거래일 값이 전날 값 그대로 실려 옵니다 —
  그래서 1일 기간에서는 변화폭을 `null` 로 둡니다. 0 으로 두면 "변화 없음"으로 읽힙니다.
- **히트맵 색 기준(clamp)은 손으로 정하지 마세요.** 시장마다 변동폭이 다릅니다
  (1일 등락률 중앙값: 한국 3.2%, 미국 0.8%). `generate-sector-rs.js` 가
  |등락률| 85분위로 계산해 데이터에 넣습니다.
